# Tools

Enabled MVP: `web_search`, `fetch_url`, `knowledge_search`, `calculator`, `current_datetime`, `structured_output`.

Disabled Phase 4 seeds: gmail, slack, notion, calendar, crm, external_database, custom_http, custom_code.

Every tool: Pydantic schemas, timeout, max response size, risk level, approval mode.
