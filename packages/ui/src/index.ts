/**
 * Shared UI package.
 *
 * Phase 1: the shadcn/Radix primitives live in apps/web/components/ui to keep
 * the setup simple. When a second app appears, move framework-agnostic
 * primitives and design tokens here.
 */
export {};
