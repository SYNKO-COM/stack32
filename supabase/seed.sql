-- Stack32 — local development seed.
-- Only non-user global data. The tool catalog is seeded by migration
-- 20260804000008_tools.sql so hosted deployments get it too; this file is
-- intentionally minimal and must never contain user UUIDs or personal data.
--
-- To create demo agents for a real local user, use a development script that
-- accepts an existing auth.users id (see docs/LOCAL_DEVELOPMENT.md).

select 1;
